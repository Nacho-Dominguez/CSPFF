<?php
/**
 * @todo - Rewrite this to use JQuery for AJAX, similar to the way
 * A25_Form_Record_Course does it. 
 */
?>

<script language="javascript" type="text/javascript">
<!--
function closeBroadcast(broadcast_id){
	document.getElementById('broadcast' + broadcast_id).style.display = "none";
	var ajaxRequest;  // The variable that makes Ajax possible!

	try{
		// Opera 8.0+, Firefox, Safari
		ajaxRequest = new XMLHttpRequest();
	} catch (e){
		// Internet Explorer Browsers
		try{
			ajaxRequest = new ActiveXObject("Msxml2.XMLHTTP");
		} catch (e) {
			try{
				ajaxRequest = new ActiveXObject("Microsoft.XMLHTTP");
			} catch (e){
				// Something went wrong
				alert("Your browser broke!");
				return false;
			}
		}
	}
	var params = "broadcast_id=" + broadcast_id;
	ajaxRequest.open("POST","<?php echo A25_Link::to('/administrator/hide-broadcast')?>");
	ajaxRequest.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
	ajaxRequest.setRequestHeader("Content-length", params.length);
	ajaxRequest.setRequestHeader("Connection", "close");

	ajaxRequest.send(params); 
}
//-->
</script>